@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(whisper-cpp REQUIRED)
#find_dependency(nlohmann_json REQUIRED)
#find_dependency(spdlog REQUIRED)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/VoiceCommandTargets.cmake")

# Set variables for backwards compatibility
set(VoiceCommand_VERSION @PROJECT_VERSION@)
set(VoiceCommand_VERSION_MAJOR @PROJECT_VERSION_MAJOR@)
set(VoiceCommand_VERSION_MINOR @PROJECT_VERSION_MINOR@)
set(VoiceCommand_VERSION_PATCH @PROJECT_VERSION_PATCH@)

check_required_components(VoiceCommand)
